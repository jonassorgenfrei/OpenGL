#pragma once

// Backward-compatible include for projects that used the old experimental
// quaternion camera header. The production implementation now lives in
// camera.h and is the default Camera type.
#include "camera.h"
